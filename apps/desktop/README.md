# Desktop Shell Placeholder

The current runnable product surface is the local relay plus the Svelte frontend.

This directory is reserved for the Tauri desktop shell that will wrap that local surface as the desktop-first product matures. The repository shape includes it now so future implementation work can land in the intended monorepo location without another structural migration.
